import { CalcIndexListPipe } from './calc-index-list.pipe';

describe('CalcIndexListPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcIndexListPipe();
    expect(pipe).toBeTruthy();
  });
});
